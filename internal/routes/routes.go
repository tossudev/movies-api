package routes

import (
	"database/sql"
	"net/http"
)

func New(database *sql.DB) http.Handler {
	// TODO: Initialize repositories, services, and handlers,
	// register routes, apply middleware, and return the router.
	return nil
}
